__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# Databases are build output here — `carbonstack init` recreates them, and the
# demo one is rebuilt by scripts/build_dashboard_data.py on every run.
*.db
*.db-wal
*.db-shm

# ...except the demo database, which the read-only Vercel deployment serves.
!dashboard/pilot.db
public/
